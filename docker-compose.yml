services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shanyraq-frontend
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: 3000
    restart: unless-stopped
