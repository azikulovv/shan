services:
  shan-tg-bot:
    image: node:24-bookworm-slim
    container_name: shan-tg-bot
    build:
      context: ./telegram-bot
      dockerfile: Dockerfile
    env_file:
      - telegram-bot/.env
    restart: unless-stopped
