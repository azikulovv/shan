services:
  telegram-bot:
    image: node:24-bookworm-slim
    container_name: telegram-bot
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
