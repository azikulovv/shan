# shanyraq

## Запуск проекта

Из корня проекта:

```bash
npm run dev
```

Команда одновременно запускает:

- backend: `http://localhost:5000`
- frontend: `http://localhost:3000`

Если порт `3000` занят, Nuxt автоматически выберет следующий свободный порт и покажет его в терминале.
